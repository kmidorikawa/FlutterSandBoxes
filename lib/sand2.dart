import 'package:flutter/material.dart';

class Sand2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("sand2"),
      ),
      body:null

    );
  }
}
