import 'package:flutter/material.dart';

class Sand1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("sand1"),
      ),
      body: null
    );
  }
}
