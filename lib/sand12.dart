import 'package:flutter/material.dart';

class Sand12 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("sand12"),
      ),
      body:null

    );
  }
}
